import { createStore } from 'vuex'
import { layoutModule } from './layout'

export default createStore({

  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    layout: layoutModule
  }
})
